rules_version = '2';

// MODO DESARROLLO — abrir para MVP sin login.
// TODO: Restaurar reglas de producción cuando se implemente Firebase Auth.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
